function socketHandler(io) {

    io.on("connection", (socket) => {

        // join user's room
        // receive messages
        // typing
        // disconnect

    });

}

module.exports = socketHandler;